

# 1️⃣ Variables (Static / Instance)

**1.** create a java application where we have 1 class it contains **2 static variables then print those variables under main method**

**2.** create a java application where we have 1 class it contains **2 static and 2 instance variables then print these variables under main method**

**3.** create a java application where we have 1 class it contains **1 static and 2 instance variables and a non static method having no parameters and returns nothing then print all these variables under this method and invoke this method under main method**

---

# 2️⃣ Methods

**4.** create a java application where we have 1 class it contains **1 instance variable and a non static method m1 having integer as params and return nothing and a static method m2 having bool and float as params and return nothing then invoke these methods under main method**

---

**5.** create a java application where we have 1 class it contains **1 instance variable and a static method m1 having string as params and returns int and a non static method m2 having bool as params and return nothing then invoke this method under main method**

---

**6.** create a java application where we have 1 class it contains **a non static method m1 having float,int as params and return boolean and a non static method m2 having string,double as params and returns float then invoke these methods under main method by providing dynamic inputs**

---

**7.** create a java application where we have 1 class it contains **a non static method m1 having no params and returns int and a static method m2 having integer as params returns nothing then invoke these methods under main method, invoke m2 method by passing m1 method return value as params**

---

**8.** create a java application where we have 1 class it contains **2 static and 2 non static variables, 2 non static methods having no params and returns nothing then print the global variables under both the methods and invoke these methods under main method with respect to 2 different objects by providing dynamic inputs**

---

**9.** create a java application where we have 1 class it contains **1 static and 1 non static variable and static method m1 having int as params and returns nothing and a non static method m2 having float as params and returns string then invoke these methods under main method by providing dynamic inputs**

---

**10.** create a java application it contains **a non static method having params and return type and a static method having params and return type then invoke these methods under main method by providing dynamic inputs**

---

**11.** create a java application where we have 1 class it contains **1 static and 2 instance variables and a non static method m1 having short as params and returns long and a static method m2 having int and boolean as params and returns double invoke all these properties under main method by providing dynamic inputs**

---

**12.** create a java application where we have 1 class it contains **1 non static variable and a non static method m1 having params and return type and a static method m2 having params and return type then invoke m1 under m2 and m2 under main method by providing dynamic inputs**

---

**13.** create a java application where we have 1 class it contains **1 instance variable, 1 non static method having params and return type, 1 static method having params and return type, another non static method m3 having params and return type then invoke m1 under m2, m2 under m3 and m3 under main method by providing dynamic inputs**

---

**14.** create a java application where we have 1 class it contains **a static method m1 having no params and returns boolean and a non static method m2 having params and return type then invoke these methods under main method, if m1 returns true invoke m2 else print a user friendly message by providing dynamic inputs**

---

**15.** create a java application where we have 1 class it contains **1 instance variable, 2 static and 2 non static methods having params and return type then invoke non static methods under static methods one each and static methods under main method by providing dynamic inputs**

---

**16.** create a java application where we have 1 class it contains **1 instance variable, 1 static and 1 non static method having params and return type then invoke these methods under main method without storing the object by providing dynamic inputs**

---

**17.** create a java application where we have 1 class it contains **1 non static method having params and return type and a static method having object as params and returns nothing then invoke m1 under m2 and m2 under main method by providing dynamic inputs**

---

**18.** create a java application where we have 1 class it contains **3 non static methods having params and m2 returns the return value of m1, m3 returns the return value of m2 then invoke all these properties by providing dynamic inputs under main method**

---

**19.** create a java application where we have 1 class it contains **a static method having return type and no params and a non static method having params and return type then invoke these methods under main method without storing or creating object directly by providing dynamic inputs**

---

**20.** create a java application where we have 1 class it contains **all types of methods then invoke any 1 of the method under main method and rest under another methods by providing dynamic inputs**

---

# 3️⃣ Mini Project Type Questions

**21.** create a java application where we have **class insta it contains 3 fields like username, password and email then provide details for these while creating a user and we have a non static method profileInfo which displays the user details then create 3 users and display their details**

---

**22.** create a java application where we have **1 class batch 251 here user need to enter data of 5 boys and 5 girls and have a method called display which displays student data then if user wants to see boys data display boys data, if user wants to see girls data display girls data, if user wants to see both display both**

---

# 4️⃣ `this` Keyword

**23.** cja where we need to **satisfy this keyword at all levels**

---

**31 create a java application where we have 1 class it contains 1 instance variable and 1 non static method then satisfy this keyword at variable level and method level**

---

**32.** create a java application where we have 1 class it contains 1 instance variable and 2 parameterized constructors then invoke these properties with respect to single memory space

---

# 5️⃣ Constructors

**24.** create a java application where we have **1 class in that create 1 instance variable and a non static method with params and return type and a default constructor then call all these properties under main method**

---

**25.** create a java application where we have **1 class it contains 1 instance variable and a parameterized constructor then invoke these properties under main method**

---

**26.** cja 1 class it contains **2 instance variables without initialization then initialize these variables while creating object and display them under a non static method display by providing dynamic inputs**

---

**27.** cja 1 class **1 instance variable, 1 default constructor, 1 single parameterized constructor, 1 double parameterized constructor, 1 triple parameterized constructor then invoke default under single, single under double, double under triple and triple under main method**

---

**28.** cja 1 class it contains **1 instance variable and an object parameterized constructor then invoke this under main method**

---

**29.** create a java application where we have **2 instance variables without initialization then initialize them while creating object and display both default values and user given values using object parameterized constructor**

---

**30.** create a java application where we have **1 instance variable and a default constructor, single parameterized constructor, double parameterized constructor and object parameterized constructor then invoke single under double, double under object and rest under main method**

---
